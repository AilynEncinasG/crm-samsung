# Scripts SQL

- `completo_samsung.sql`: script original compartido.
- En Somee normalmente ya tienes creadas las bases. Si no tienes permiso para ejecutar `CREATE DATABASE`, entra a cada base por separado y ejecuta solo las tablas/triggers correspondientes.
